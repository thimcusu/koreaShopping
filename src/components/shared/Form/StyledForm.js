import styled from 'styled-components';

import { device } from '../../../contrants/deviceSizes';
import mixins from '../../../assets/style/mixins';

export const StyledFormGroup = styled.div.attrs((classNames = 'form-group'))``;
